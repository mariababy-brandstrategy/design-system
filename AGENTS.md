# 여기는 마리아 사내 웹 UI 의 규칙 정본(SoT)이다

`docs/web-ui-guidelines-v1.md`(진입점) + `docs/internal-service-header-v1.md`(헤더 상세)
+ `tokens/design-tokens.css`(토큰 값). 다른 어디와 값이 갈리면 **여기가 우선한다.**

소비 구조: 이 문서의 규칙을 `dyshin-maria/maria-ui` 의 비공개 레지스트리 `@maria/*` 가
코드로 구현하고, 사내 현역 6앱(console·popo-studio·mou-admin·hub·labs·sns)이 그걸 설치해 쓴다
(claim 은 2026-08-22 서비스 폐기 — 2026-08-25 검사 모집단에서도 제외됨. 되살리면 재등록).
hub 는 `components.json` 이 없어 레지스트리 부품은 손복사본이고, **토큰은 design-system
서브모듈**(`hub/design-system` → 이 repo)로 소비한다 — 토큰 개정 시 hub 는 서브모듈 포인터를
올린다(2026-08-25 실측 정정: 종전 "토큰도 손복사" 기술은 사실과 달랐다).
(2026-08-12 정정: 오래 "5앱"으로 적혀 있어 **labs·sns 를 빠뜨릴 위험**이 있었다.
앱 목록의 기계 정본은 `maria-ui/scripts/ui-audit.config.mjs` 의 `APPS` 다.)

## 문서를 고칠 때

문서만 고치면 **아무것도 바뀌지 않는다.** 규칙을 바꿨으면 구현과 앱까지 따라가야 한다:

1. `maria-ui` 의 `registry/` 구현 갱신
2. `maria-ui/scripts/ui-audit.mjs` 에 그 규칙을 강제하는 검사 추가·갱신
3. 6앱 재동기(5앱은 `shadcn add --overwrite`, hub 는 부품 손복사·토큰은 서브모듈 bump)
4. `node ~/maria-ui/scripts/ui-audit.mjs --static-only` 로 전수 확인
5. 문서 끝 **변경 이력에 버전 한 줄** 추가

## 정본에서 벗어나는 변경 요청을 받으면

바로 적용하지 말고 먼저 "이건 정본의 <어느 항목>과 다르다"를 알리고 확인을 받는다.
디자인 변경은 전부 에이전트를 거친다는 전제로 **자동 순찰을 두지 않기로 했다**
(사용자 결정 2026-07-31). 감시자가 없으므로 이 확인 절차가 유일한 게이트다.

## 브랜드 자산 주의

로고는 전용 워드마크다. **어떤 폰트로도 글자를 타이핑해 흉내 내지 않는다.**
정본 SVG = `assets/logos/svg/`, 코드 소비는 `@maria/brand-logo`.
어두운 배경에서는 100% 흰색(`text-white`)이고 아이보리(`text-on-dark`)는 금지다.

## 문서 레이어 — HTML→PDF 문서는 「문서를 고칠 때」 5단계 밖이다 (2026-09-03 신설 · 2026-09-22 개정)

토큰을 소비하는 **정적 문서**(브라우저로 읽고 A4 로 인쇄하는 HTML→PDF)는 사내 웹 UI 가 아니다.
registry 부품을 쓰지 않고 6앱과 재동기할 것도 없다.

- **면제 기준은 바뀐 파일이다.** `styles/document-base.css` 와 `docs/` 의 문서 소스만 바뀌면 5단계를 발동하지
  않는다. 같은 작업이 `tokens/design-tokens.css`·`docs/web-ui-guidelines-v1.md`·이 파일을 만지면 그 부분은 5단계로 판정한다.
- **우선순위**: 공유 축(색·대비·굵기·크기 스케일·간격 스텝·폰트)은 이 파일 첫 문단의 정본이 우선한다 →
  `styles/document-base.css`(문서 구현 — 절 리듬·판면·인쇄만 여기가 정한다) → 문서 자체 override(예외는 사유 주석과 함께).
  **값의 원본은 토큰이다**(사용자 결정 2026-09-22): 색상 편 PDF 는 토큰 값을 인쇄물 독자에게 설명하는 소비자이며,
  어긋나면 PDF 쪽을 다시 찍는다(2026-04 제정 때는 PDF 가 먼저였으나 지금 운영은 토큰 중심 — README 「유지보수」 절도 같은 문장).
- **포함 순서 고정**: `tokens/design-tokens.css` → `styles/document-base.css` → 문서 override. base 는 스코프 루트 `.doc`
  아래에서만 작동한다 — `.doc` 하위 요소 기본값(h1~h4·p·a·목록·표·figure·footer) + opt-in 기구(`.sheet`·`.sec-head`·`.cont` 등).
  전역 리셋은 없지만 `@page` 는 전역 인쇄 규칙이라 스코프할 수 없다 → base 는 문서 전용이며 앱 번들에 넣지 않는다.
- **적용되는 정본 축**: §1-1 로고 · §1-4 색·대비(값 기준) · §1-9 굵기·스케일 · §1-10 간격 스텝 · Pretendard 고정 버전
  (문서 임베드용 서브셋 = `assets/fonts/pretendard/`, 웹 앱은 §1 표의 CDN).
- **적용 대상 부재**(§1-12 셸과 같은 이유 — 오갈 화면이 없다): 헤더·본문 너비·`<main>` 여백·페이지 제목 블록 ·
  §1-2 아이콘 세트 · §1-3 탭 제목 서식 · §1-5 다이얼로그 · §1-7 배지 · §1-8 행동 라벨 · §1-13 폼 버튼 · §1-14 세그먼트(상호작용 축 전부) ·
  §1-9 「목록 화면의 3층 위계」·§1-16 앱 안 산출물 면(앱 화면의 층 — 문서의 절 리듬은 base 「절 리듬」이, 1장 배부물 판면은 아래 「1장 배부물」이 정한다).
- **1장 배부물 — 한 장짜리 HTML→PDF 문서의 판면**(2026-09-22 신설, 판정표 ㉤ `maria-ops-archive/handoffs/print-layer-rules/2026-09-22-print-layer-audit.md` ·
  사용자 결정 2026-09-22 + Codex 4h·4b): document-base(여러 절 A4 세로)도 hub `POSTER_FRAME`(앱 캔버스)도 아닌 **문서 층의 세 번째 소비 유형**이다.
  현재 소비자 = 대구 배부 안내문 v2.6 · 대구 환자 공지(둘 다 `maria-ops-archive/handoffs/daegu-fertility-notice/`, repo 밖) — repo 안 0.
  - **글자 크기 = §1-9 스케일의 pt 환산**(9 · 10.5 · 12 · 13.5 · 15 · 18 · 22.5 · 27 · 36pt = 12~48px × 0.75, 동률은 위쪽). 값의 원본은 하나다(문서
    레이어 원칙·약식판 v1.2 전례). 밀집 조건의 11px = **8.25pt**. **표식·각주에 한해 7.5pt(10px) 허용**(사용자 결정 2026-09-22 — ⚠ 근거는 대구
    헤드리스 PDF·화면 판정뿐이고 **실물 출력 확인은 없다**. 종이가 화면보다 촘촘하다는 판단이지 실측이 아니다. 실물에서 안 읽히면 이 허용을 닫는다).
    대구 v2.6 대조 = 22→22.5 · 12→12 · 9.5→9 · 8.5→8.25 · 7.5→7.5 — 값 비교일 뿐 **이행은 아니다**(배부 대기 문서라 재조립하지 않는다. 미이행·개별
    예외 후보로 기록, 다음 판에서 수렴).
  - **위 우선순위와의 관계**(Codex 4b): 공유 축(글자 스케일·굵기·색·폰트)은 그대로 정본이 우선한다. 아래 프로파일은 **이 소비 유형의 판면·간격·행간에
    한정한 관측 권장안**이고 base 「절 리듬」·「판면」을 대체하지 않는다(한 장 문서는 절이 없어 그 규칙이 닿지 않을 뿐이다). base 구현은 바꾸지 않는다.
  - **판면·간격·행간 = 대구 v2.6 프로파일을 관측된 권장안으로 둔다**(공통 의무 아님 — 소비자 2건뿐, Codex 4h): 시트 297×210mm 고정·여백은 시트
    패딩 · 격자와 기준선을 템플릿 머리 주석에 명시하고 **각주는 소속 블록의 왼쪽 선** · 간격 사다리 mm 1·2·4·6·12 를 위계에 매핑(1 상자 안 · 2 제목→내용·
    표 셀 · 4 내용→각주·칸 틈 · 6 띠 사이 · 12 좌우 여백) · 행간 1.2(제목)/1.4(라벨·상자·각주·표 = 토큰 tight)/1.5(본문 문단) — **이 프로파일에서는**
    base 1.7 을 쓰지 않는다(한 장에 담기 위한 선택이지 금지가 아니다). 토큰 신설 없음(1.2·1.5 는 판면 override 값).
  - **검증 두 갈래**(Codex 4b 정정 — 조립기 검사와 출력 검증은 다른 것이다): ① 조립기 검사 = 서브셋 글꼴 실제 cmap 커버리지·미치환 자리표시(대구 두 조립기가
    각자 돈다 — `docs/assemble_lib.py` 공통 검사는 창고 문서엔 안 걸린다) ② 출력 검증 = 별도 실행(`measure_sheet.sh` 방식 DOM `getBoundingClientRect`
    overflow 0 · `pdfinfo` 쪽수 1 · 출력 글꼴 = 화면 글꼴 육안) — 자동이 아니라 회차마다 사람이 돌린다.
  - **base opt-in 기구(`.leaf` 류)는 두지 않는다** — 세 번째 소비자(다음 배부물 또는 hub 도구화)가 생길 때 판단. hub 는 문서 base 를 소비하지 않으므로
    (POSTER_FRAME px 캔버스) hub 도구화는 이 절이 아니라 §1-16 「조판 위계」 대상이다(대구 v2.6 을 hub 프레임에 넣으면 본문 예산 49px 초과 — replan §12).
- **쪽 번호·표지**(base v1.2, 사용자 결정 2026-09-22): 쪽 번호 = `@page` 여백 상자 하단 오른쪽 12px muted 숫자, 물리 쪽 번호(표지 = 1 이라
  본문은 2 부터 — PDF 뷰어의 쪽 표시와 어긋나지 않게), 표지에는 없음. 표지 밴드 = base opt-in `.cover`(문서 차이만 override) · 표지 없는
  문서(약식판 `.band`)는 첫 쪽부터 번호. 서브셋 글꼴을 임베드하는 문서는 `:root{--doc-font-family:…}` 로 쪽 번호 글꼴도 같은 서브셋을 쓴다.
- **절 번호의 정의**(2026-09-22, 사용자 판정 + Codex 4h×2 — 종전엔 정의가 없어 문서마다 "01"·"§1"·"1" 로 갈렸다):
  절 번호는 **독자가 인용할 때 부르는 식별자("3절 참조")** 이지 장식이 아니다. 표기는 **"자연수."** 하나(`<span class="no">3.</span>`,
  조립기가 검사) · 서문("이 문서에 대해")·부록성 절은 빈 번호(`<span class="no"></span>`)로 제목만 두며 본문 절 사이에 끼우지 않는다 ·
  소제목(h3)에는 번호를 달지 않는다 · 차례·본문 상호참조도 같은 표기("3절의 표"). 시각 = 제목과 같은 30px·600, 색만 muted,
  구분 신호는 색 + 마침표, 열은 내용 폭·gap 12(규칙 본문과 근거 주석 = `styles/document-base.css` 「절 리듬」). 외부에서 인용할 때는
  문서명·판·절 번호를 함께 적는다(문서끼리 같은 번호가 있다).
- **소비자**(4, 2026-09-22 base v1.2 기준): `docs/브랜드-가이드-색상-v2.3` (소스 `docs/color-guide-src/`) · `docs/브랜드-가이드-공간-v1.2`
  (소스 `docs/space-brand-guide-src/`) · `docs/브랜드-가이드-화면-v1.2` (소스 `docs/screen-brand-guide-src/`, 외부 협력사·사내 개발용 화면 기준) ·
  `docs/브랜드-디자인-규격-약식판-v1.2` (소스 `docs/brief-spec-src/`, 2026-09-22 편입 — 요약본이라 본편과 다르면 본편 우선. 마지막 쪽 관련 가이드 표지 썸네일 3장은
  `assemble.py --covers` 가 docs/ 직하 현행판 PDF 1쪽에서 다시 뜬다 — 가이드의 판이 오르면 같이 돌리고 figcaption 의 판·쪽수 문구는 손으로 맞춘다).
  각 소비자 = `{html,pdf}` 한 쌍, 생성 = `python3 docs/<slug>-src/assemble.py --pdf`(공용 로직·검사 = `docs/assemble_lib.py`:
  서브셋 글꼴 실제 cmap 커버리지 · 절 번호 표기 · 옛 부서명 '브랜드전략부' 0건 · 절차용 표식 · 미치환 자리표시).
  base 를 고치면 소비자 전부를 재조립해 렌더 diff(쪽수·쪽별 변화 영역)로 확인하고 이 목록을 갱신한다. 기계 검사는 두지 않는다(소비자 4건 — 정직 표기).
  base 머리 주석의 소비자 줄은 이 절을 가리키는 포인터다(2026-09-22 정리).
- **문서 이름 규칙**(사용자 결정 2026-09-10, 2026-09-22 첫 적용):
  ① 겉과 속 일치 — 파일명 = 표지 제목의 단어·순서 그대로 + `-v판`. 공백·가운뎃점은 하이픈. HTML `<title>`·PDF 메타도 같은 제목. 수신자·용도는 부제에.
  ② 시리즈 우선 — 가이드 제목은 `브랜드 가이드 · {권}`(색상/공간/화면). 표지에는 시리즈명 소라벨 + 권 이름을 크게, 부제에 적용 대상·정본 관계
     (색상 = 인쇄물·슬라이드용, 웹 화면은 화면 편 — 실무자용을 웹 정본으로 오지정했던 2026-08-23 사고 방지).
  ③ 판 = 효과 기준 — 허용하던 결과를 금지하거나 새 필수 조건이 생기면 M 올림(m=0), 설명·제목·레이아웃·항목 추가는 m 올림. 날짜는 파일명에 없다.
     2026-09-22 통일 = m 올림(색상 v2.2 · 공간 v1.1 · 화면 v1.1 — 값·규칙 변화 없음). 같은 날 쪽 번호·표지 승격(base v1.2)도 레이아웃 추가라
     m 올림(색상 v2.3 · 공간 v1.2 · 화면 v1.2 · 약식판 v1.1). 약식판 v1.2(같은 날) = 썸네일 현행화 + 글자 크기 §1-9 스케일 수렴 — 외형만이라 m. 외형이 바뀌어도 허용/금지가 안 바뀌면 M 이 아니다. 협력사 재전달은 값이 바뀌는 판에서만(사용자 결정).
  ④ `docs/` 직하에는 현행판만. 옛 HTML·PDF 는 `docs/archive/` 로 그대로 옮긴다(재조립 금지 — 배포된 파일과 byte 동일). 계약 링크는 커밋 고정이라 무관.
  ⑤ 최신 링크 = `docs/README.md` 의 고정 안내(현행판·대상·구판 연결). 다른 문서는 거기로 링크한다.
  ⑥ 소스 폴더 슬러그(`color-guide-src` 등)와 템플릿 파일명(`color-guide.template.html`, 판 없음)은 제목·판과 무관한 고정 ID — 개명하지 않는다.
  ⑦ 문자 — 한글 허용·NFC, 공백과 `\ / : * ? " < > | ·` 금지, 구분은 하이픈, `.` 은 판·확장자에만. 메일 첨부 → Windows 열기 검증은 첫 발송 때 1회(사용자).
  ⑧ 생성물 전부에서 옛 부서명 '브랜드전략부' 0건(조립기 assert).
- **폰트 서브셋에 글자를 늘릴 때**: `chars.txt` 에 넣는 것만으로는 부족하다 — **원본 Pretendard 에 그 글리프가 실제로 있어야** 한다
  (2026-09-08 실측: `✕` U+2715 · `⬇` U+2B07 은 원본에 없어 서브셋에 들어갈 수 없고, 조용히 시스템 폰트로 떨어진다).
  커버리지 검사는 `chars.txt` 가 아니라 **woff2 의 실제 cmap** 을 본다 — 2026-09-22 부터 `docs/assemble_lib.py` 가 4 소비자 공통으로 검사한다.
- **문서 레이어 규칙 공백 판정표**(2026-09-22, 절 번호·리드·h4·kv·figcaption·표·꼬리·표지 밴드·쪽 번호·판 표기·`.rules`·줄 폭 캡·약식판 신규 부품 등
  축마다 "규칙 필요 / 문서 재량 / 이미 정본에 있음" 판정) = `~/maria-ops-archive/handoffs/docbase-rules/2026-09-22-secnum-audit.md`. 거기 미결이던
  표지 밴드 `.cover` 승격 · 쪽 번호는 같은 날 사용자 결정으로 base v1.2 에 확정(위 「쪽 번호·표지」). 웹 UI 공백 ㉠~㉥은 web-ui-guidelines v1.60,
  거기 미수렴이던 ㉡·㉤ + sns 인계 4건은 v1.63(인쇄물 층 판정표 = `maria-ops-archive/handoffs/print-layer-rules/2026-09-22-print-layer-audit.md`).
- 최초 신설(2026-09-03)과 2026-09-22 개정은 이 파일을 바꾼 것이므로 `ui-audit.mjs --static-only` 를 1회 돌려 웹 UI 영향 없음을 확인하고 결과를 커밋 메시지에 남긴다.

## UI 작업의 완료 조건

**UI 를 건드린 작업은 `node ~/maria-ui/scripts/ui-audit.mjs --static-only` 가 통과해야
끝난 것이다**(권고가 아니라 완료 조건).
사람이 의도하지 않은 시각 변화는 주로 옆문으로 들어온다 — **오래된 브랜치 병합 ·
Tailwind/의존성 업그레이드 · codemod · GitHub 웹 편집**도 UI 변경으로 친다.
사전 확인 모델은 그 경로들을 스스로 막지 못하므로, 이 확인이 마지막 그물이다.
